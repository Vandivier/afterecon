{
  "buildCommand": "apt-get update && apt-get install -y python3-distutils python3-dev build-essential && npm run build",
  "installCommand": "apt-get update && apt-get install -y python3-setuptools && npm install",
  "framework": "nextjs"
}
